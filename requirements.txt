cryptography
py-cpuinfo
tqdm
torchvggish
regex
docker
ebooklib
python-pptx
python-docx
fastapi
hf_xet
beautifulsoup4
nagisa
pymupdf
pymupdf-layout
pytesseract
unidic
hangul-romanize
iso639-lang
soynlp
jieba
pycantonese
pypinyin
pythainlp
mutagen
PyOpenGL
phonemizer-fork
pydub
unidecode
langdetect
phonemizer
indic-nlp-library
stanza==1.10.1
argostranslate==1.11.0
pandas>=1.0,<4.0
gradio>=5.49.1
pyannote-audio==3.4.0; python_version < '3.12'
pyannote-audio>=4.0.0; python_version >= '3.12'
huggingface_hub>=0.36.2,<1.0; python_version < '3.12'
huggingface_hub>=0.36.2; python_version >= '3.12'
torch==2.7.1; python_version < '3.12'
torch==2.11.0; python_version >= '3.12'
torchaudio==2.7.1; python_version < '3.12'
torchaudio==2.11.0; python_version >= '3.12'
transformers==4.57.6; python_version < '3.12'
transformers==4.57.6; python_version >= '3.12'
coqui-tts[languages]==0.27.5
basedpyright
ext/py/num2words
ext/py/demucs

# Fish Speech 1.5 — zero-shot voice cloning (CC-BY-NC-SA-4.0, non-commercial only)
# Not on PyPI. Install via git URL (pip will handle it) or clone + pip install -e .[cu128].
# Note: torch.compile is disabled on Windows/macOS (Triton not available).
# Linux system deps required: apt install portaudio19-dev libsox-dev ffmpeg
git+https://github.com/fishaudio/fish-speech.git@v1.5.1
ormsgpack
descript-audio-codec
einops>=0.7.0

# CosyVoice 2 / 3 — zero-shot voice cloning (Apache 2.0, commercial use allowed)
# Not on PyPI. Clone once into third_party/CosyVoice, then install its requirements:
#   git clone --recursive https://github.com/FunAudioLLM/CosyVoice third_party/CosyVoice
#   pip install -r third_party/CosyVoice/requirements.txt
#   # Restore project deps pinned higher than CosyVoice's requirements.txt:
#   pip install "numpy>=2.0" "pydantic>=2.10" "fsspec>=2024.6"
# Linux system deps: apt install sox libsox-dev
# Note: GPU ONNX acceleration (onnxruntime-gpu) is Linux-only; Windows uses CPU ONNX.
#       PyTorch LLM backbone still runs on CUDA on Windows.
# Note: vLLM and TensorRT acceleration are optional; disabled by default in cosyvoice.py.
